/*
** EPITECH PROJECT, 2021
** Indie
** File description:
** RaylibWindowTests
*/

#include <gtest/gtest.h>
#include "../Tests/Lib/XRaylib/Raylib/Window/Window.hpp"

/*
TEST(WindowTest, initWindowTest)
{
    try {
        Raylib::Window test;
        test.initWindow(100,100,"test");
    } catch (std::exception &ex) {
        EXPECT_STREQ("ERROR: Invalid Initialize window and OpenGL context", ex.what());
    }
}

TEST(WindowTest, windowShouldCloseTest)
{
    Raylib::Window test;
    EXPECT_EQ(false, test.windowShouldClose());
}

TEST(WindowTest, closeWindowTest)
{
    try {
        Raylib::Window test;
        test.closeWindow();
    } catch (std::exception &ex) {
        EXPECT_STREQ("ERROR: Invalid Close window", ex.what());
    }
}

TEST(WindowTest, setWindowIconTest)
{
    try {
        Raylib::Window test;
        Raylib::Image image("default");
        test.setWindowIcon(image);
    } catch (std::exception &ex) {
        EXPECT_STREQ("ERROR: Invalid Set the Window Icon", ex.what());
    }
}

TEST(WindowTest, isWindowFullscreenTest)
{
    Raylib::Window test;
    EXPECT_EQ(false, test.isWindowFullscreen());
}

TEST(WindowTest, toggleFullscreenTest)
{
    try {
        Raylib::Window test;
        test.toggleFullscreen();
    } catch (std::exception &ex) {
        EXPECT_STREQ("ERROR: Invalid Toggle window state:", ex.what());
    }
}
*/